fix(bmm-opt): 同步提交副本 + 补齐 tiling 兜底档（真源/副本哈希不一致是连续两轮 110 的真因）

一、真因（有哈希证据）
   判题用的**副本**一直是旧的：
     真源 bmmms-kernel-opt/kernel.asc                     722dd0ce37a4 (1495 行, SINGLE_LAUNCH=1)
     副本 bmmms-kernel-opt/submit/project/kernel.asc      b7192b89fd01 (1401 行, SINGLE_LAUNCH=0)
     副本 .research/repos/.../npu-v1/project/kernel.asc   b7192b89fd01 (1401 行, SINGLE_LAUNCH=0)
   => 上一轮提交跑的还是带 phase-2 第二 kernel 的旧版，于是误差完全一致：
      Expected 75, got 110（差值 35 = 7 个 case x 5 次迭代，每 case 多起一个
      PartialSumKernel）。
   本轮把三处同步成同一个哈希 769c1e004495：BMM_SINGLE_LAUNCH=1 =>
   directOut 恒为 true => PartialSumKernel 那段是死代码 => 一次迭代恰好 1 个 kernel。

二、顺手堵掉剩余的 abort 面（abort 会被 ReplayOnce 重放，同样多算 launch）
   - tiling 降级阶梯从 4 档加到 6 档，新增两档"基线口径"兜底：
     (16, baseNTarget) 与 (16, 64) —— 真机 15/15 用的就是 baseM=16，
     所以 tilingReady 必然可达，`连 tiler 自行决定都拿不到可用的 baseM/baseN` 那个
     Fail() 实际不可达。
   - 内部 stream 同步超时 3000ms -> 60000ms（超时会走 CheckAcl -> abort -> ReplayOnce）。
   - 新增 bmmms-kernel-opt/sync_submit.py：提交前核对"真源 vs 所有会被提交的副本"
     的哈希，--write 直接覆盖。这次的错=副本没换，以后一条命令就能挡住。

三、工具修正
   bmmms-kernel-gm/tools/launch_check.py 原来把同层级的 if/else 兄弟分支算成
   "同一代码块"，对多 transpose 分支的 kernel 误报。改成按"每个启动点最近的
   if/else 行号是否互不相同"判断互斥，现在这份 kernel 判为通过。

门禁：syntax_check 通过（无 warning/error）；api_gate 零新增 API；
      sync_submit.py 核对三处哈希一致（769c1e004495）。
