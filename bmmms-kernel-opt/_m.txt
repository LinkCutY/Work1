fix(bmm-opt): 关掉 phase 2 第二 kernel（修 launch 数超标 110→75），去掉未验证 API

基线：你本人那版 kernel.asc（bmmms-kernel-opt）。它的 tiling 用 SetBufferSpace(-1,-1,-1)
= 平台真实值，**没有**把 AIC L1 写成 512KiB —— 这一条已按行号核对过。

根因（这版自己的 VERIFY 第 4 条预告过）：
  directOut = (BMM_SINGLE_LAUNCH != 0) || (nmwin == 1 && groups == 1)
  if (!directOut) PartialSumKernel<<<blocks>>>   ← 第二个 kernel
凡 nmwin>1 或 groups>1 的 case 每次迭代起 2 个 kernel ⇒ "Expected 75, got 110"
（差值 35 = 7 个 case × 5 次迭代）。上一轮我把 BMM_GROUPS_MULT 从 1 改成 2，
反而让更多 case 落进这条路 —— 已回退。

改动：
- BMM_SINGLE_LAUNCH 0 -> 1：强制 groups=1、每个 task 把行和原子累进 y[batch]、
  不再启动 phase 2 ⇒ 一次迭代恰好 1 个 kernel。副作用（已知可接受）：
  放弃 N 方向并行；原子加末位不可复现（离线量化极差 ≤3.1e-2，容差下限 ≥8.2）。
- 去掉唯一的 UNKNOWN API：platform->GetCoreNumAiv() -> 常数 2
  （依据：基线真机实测 AIV 的 GetBlockIdx() ∈ [0,2*blocks)）。api_gate 现在零新增 API。
- 保留上一轮的核数动态部分：ratio = min(BMM_TASK_RATIO, 硬件比例)、
  workers = ratio*blocks（修掉 host 写死 2U*blocks 与 kernel 用 BMM_TASK_RATIO
  不一致、ratio=1 时一半 task 没人领的静默偏小 bug）、chunkWidth 的双缓冲上界。
- VERIFY 增补 11)（本轮修复 + 原子对齐的文档依据 + 残留风险）与 12)（不再引入未验证 API）。

门禁：syntax_check 通过（无 warning/error）；api_gate 零新增 API。
